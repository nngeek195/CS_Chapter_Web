import React from 'react';

// Stub file to prevent stale editor cache errors if app/page.tsx is open in an unsaved buffer.
// The home page on disk does not use this component.
export default function WaterHeroOrb() {
  return null;
}
